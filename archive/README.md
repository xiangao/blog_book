# Archived sources

Superseded chapter sources, kept for reference and not part of the book.

- `same-data-different-estimators.qmd` — replaced by
  `tasc-time-aware-synthetic-control.qmd`, which carries the same
  four-estimator Proposition 99 comparison plus TASC. Removed from
  `_quarto.yml`; the published URL is now
  `tasc-time-aware-synthetic-control.html`.

- `pre_agy_20260903/` — the previous, longer versions of six chapters
  (`equivalence-testing`, `gwg`, `likert-scale-variance`, `more-cre`,
  `mundlak-device`, `poisson-iv-fe`), replaced 2026-09-03 by shorter
  rewrites. Kept in a dated subdirectory rather than flat because the
  basenames still match live chapters. Several analyses exist only here —
  gwg's RIF/quantile decomposition, Lee bounds and `sensemakr` sensitivity;
  likert's exact-ceiling derivation and four-model comparison;
  equivalence-testing's Bayes-factor unit-trap demonstration. See the README
  in that directory.
